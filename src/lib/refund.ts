/**
 * 환불에 쓰는 이름들
 *
 * 화면(브라우저)과 서버가 같이 쓰는 값이라 여기 따로 둔다.
 * members.ts 안에 두면 화면 코드가 구글 접속 코드까지 끌고 들어가 빌드가 깨진다.
 */

/**
 * 환불을 기록하는 데 필요한 칸
 *
 * 처음 만든 시트에는 이 칸들이 없다.
 * 대표가 매출 화면에서 버튼을 누르면 이 이름 그대로 결제 탭에 만들어진다.
 */
export const REFUND_COLUMNS = ["환불진행상태", "환불사유", "환불신청일", "환불완료일"];

/** 환불이 어디까지 왔는지 */
export const REFUND_STAGES = ["접수", "승인", "환불완료", "반려"] as const;

/** 왜 환불했는지 — 직접 적을 수도 있게 둔다 */
export const REFUND_REASONS = ["개인사정", "이사", "부상·질병", "서비스불만", "기타"];
